##
## This container image patches sound for each specific aircraft scenario
##
ARG CONTAINER_REPO
ARG BASE_TAG
FROM $CONTAINER_REPO:$BASE_TAG

# install the sound driver
RUN    dnf -y install alsa-sof-firmware \
    && dnf -y clean all
